# This file tells Render how to build your services.
# Format is now:
# 1. A top-level 'databases' block
# 2. A top-level 'services' block

databases:
  # --- 1. PostgreSQL Database ---
  - name: real-estate-db
    databaseName: real_estate_db
    plan: free
    postgresMajorVersion: "14"

services:
  # --- 2. Django API Service ---
  - type: web
    name: real-estate-api
    env: python
    plan: free
    # Tell Render to look inside your backend folder
    rootDir: ./RealEstate
    buildFilter:
      paths:
        - RealEstate/**
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate
    startCommand: gunicorn backend_core.wsgi
    envVars:
      # --- Tell Django to use the database we just created ---
      - key: DATABASE_URL
        fromService:
          type: pserv # Changed from 'postgres'
          name: real-estate-db # Must match the database name above
          property: connectionString
      
      # --- Standard Django Settings ---
      - key: DEBUG
        value: false
      - key: SECRET_KEY
        generateValue: true # Render will create a secure key
      
      # --- Environment variables we will set manually ---
      - key: CORS_ALLOWED_ORIGINS
        value: "http://localhost:5173" # Placeholder
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false

